import 'package:flutter/material.dart';

class AppConstants {
  static const double defaultPadding = 8.0;
  static const double textBoxPadding = 12.0;
  static const double messageBubblePadding = 16.0;
  static const double defaultBorderRadius = 24.0;
  static const double circularButtonRadius = 50.0;
  static const double appBarFontSize = 17.0;
  static const double messageFontSize = 15.0;
  static const double timestampFontSize = 11.0;
  static const double appBarHeight = 56.0;

  // Brand
  static const Color kPrimary = Color(0xFF4361EE);
  static const Color kPrimaryLight = Color(0xFFEEF2FF);

  // Light
  static const Color kBackground = Color(0xFFF8FAFC);
  static const Color kSurface = Colors.white;
  static const Color kBorder = Color(0xFFE2E8F0);
  static const Color kTextPrimary = Color(0xFF1E293B);
  static const Color kTextSecondary = Color(0xFF64748B);
  static const Color kInputFill = Color(0xFFF1F5F9);

  // Dark
  static const Color kDarkBackground = Color(0xFF0F172A);
  static const Color kDarkSurface = Color(0xFF1E293B);
  static const Color kDarkBorder = Color(0xFF334155);
  static const Color kDarkTextPrimary = Color(0xFFF1F5F9);
  static const Color kDarkTextSecondary = Color(0xFF94A3B8);
  static const Color kDarkInputFill = Color(0xFF1E293B);

  // Legacy
  static const Color primaryColor = kPrimary;
  static const Color darkBackground = kDarkBackground;
  static const Color lightBackground = kBackground;
}

name: student_help_desk
description: "Öğrenci Yardım Masası - AI destekli çalışma asistanı."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.3.0
  cloud_firestore: ^6.1.1
  firebase_auth: ^6.1.3
  google_sign_in: ^6.2.1
  http: ^1.6.0
  image_picker: ^1.2.1
  permission_handler: ^12.0.1
  path_provider: ^2.1.0
  shared_preferences: ^2.3.0
  connectivity_plus: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
